<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="@color/app_default_background">

    <com.handmark.pulltorefresh.library.xlistview.XListView
        android:id="@+id/stock_favorite_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="56dp"
        android:divider="@null" />


    <RelativeLayout
        android:id="@+id/empty_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="56dp"
        android:paddingTop="121dp">
        <Button
            android:id="@+id/btn_stock_favorite_add"
            style="@style/self_select_empty_font_prompt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerHorizontal="true"
            android:background="@null"
            android:drawablePadding="14dp"
            android:drawableTop="@drawable/icon_stock_favroite_add"
            android:text="@string/add_stock_favorite_btn" />
    </RelativeLayout>


    <ProgressBar
        android:id="@+id/content_loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:visibility="gone" />

</RelativeLayout>